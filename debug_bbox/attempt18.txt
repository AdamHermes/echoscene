# Map the checkpoint and config paths if not already done
!ln -sfn /content/echoscene/scripts/checkpoint /content/echoscene/checkpoint
!rm -rf /content/config && ln -s /content/echoscene/config /content/config

# Run the evaluation with PYTHONPATH set to the project root
!PYTHONPATH=/content/echoscene /usr/local/miniconda3/envs/echoscene/bin/python eval_3dfront.py \
    --exp ../released_full_model \
    --dataset ../FRONT/ \
    --epoch 2050 \
    --visualize True \
    --room_type all \
    --render_type echoscene \
    --gen_shape True \
    --max_samples 1 \
    --start_idx 3 \
    --debug True